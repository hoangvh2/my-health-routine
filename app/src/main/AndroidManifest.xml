<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- So the player's "Xem video hướng dẫn" link can resolve a browser on Android 11+. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Reminders are notifications, never a ringing "báo thức" alarm-clock UI — see
         notify/ReminderScheduler.kt. Three permissions this needs, none of them tied
         to an account or a server: posting the notification itself (API 33+ runtime
         permission), timing it precisely enough to be useful (a settings toggle on
         API 31+, always allowed below that), and re-arming after the phone reboots. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- The app's own first network request: the player loads an exercise's YouTube
         thumbnail inline (ui/player/ExerciseThumbnail, Coil). Opening a video link in
         an external browser never needed this — that's just handing an Intent to
         another app — but fetching the preview image ourselves does. Nothing else in
         the app touches the network; there is still no server, no account. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VhHealthApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VhHealth">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VhHealth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
